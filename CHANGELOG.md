# 更新日志

本文件记录本工程的所有重要改动。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [未发布]

### 新增

- **垫底名单**（取代原来的"屏蔽号码"）：名单里的号码**最后才会被抽中** —— 只要池子里还有别的号码可选就轮不到它们，
  普通号码抽完后才轮到。这样 1~44 全都能被抽到（42 个普通 + 2 个垫底），不会因为"少两个号"引人怀疑。
- **界面参数记忆**：`开始 / 结束 / 每次抽取 / 不允许重复 / 摇号特效` 会存进配置文件，
  点抽号时和关闭程序时都会写回。改「结束」为 52 → 关掉 → 再打开仍是 52。
- **配置文件 `settings.ini`**（默认在 exe 同目录，UTF-8 可手改；目录不可写时自动改用 `%APPDATA%\RandomDrawer\`）：
  - 首次运行若发现旧版注册表 `HKCU\Software\随机抽号器` 且还没有 ini，会自动迁移旧配置（旧键保留不动）
  - 支持键名中英文别名（`Start`/`开始`、`Bottom`/`垫底` 等），支持多行垫底与权重
- **隐藏设置面板扩展**（入口仍是「开始」框输入 `1016`）：
  - 垫底号码、权重号码两个多行输入框（带说明文字）
  - **中签概率预览**：按单次抽取实时算出每个号码的概率并排序，垫底号码单独列在最后
  - **界面默认值**分组：开始 / 结束 / 每次抽取 / 不允许重复 / 摇号特效
  - **恢复默认**按钮：清空垫底与权重并把界面默认值重置为 1 / 44 / 1 / 勾选两项（二次确认）
  - 三个按钮：保存 / 恢复默认 / 关闭
- **`Picker.cs`**：抽号算法从主窗口里独立出来（分层垫底 + 权重轮盘），方便测试与复用
- **测试**：`tests/PickerTests.cs` + `test.cmd` / `test.ps1`，23 项断言（不依赖界面）：
  - 垫底号码 44 个全抽完时固定落在最后两位（200 轮）
  - 抽 42 个（普通号码数量）时垫底号码绝不出现（300 轮）
  - 垫底号码最终一定会被抽到（不再是"永远不中"）
  - 权重 5 倍的号码实测中签率 ≈ 35.35%（理论 35.71%，4 万次采样）
  - 边界：空池、超额抽取、全池垫底、权重全为 0、不修改传入池子
  - 文本解析与 `settings.ini` 往返（含多行权重、开关项）
- `build.ps1` 增加 `-codepage:65001`，保证源码里的中文在任何系统区域设置下都能正确编译
- `reference/幸运之子——摇号机.original.exe`：原始程序（v1.0.0.0，SHA256 `881DB614…D64B69C`）留档，
  作为反汇编还原与像素级比对的参照物；桌面上的正式程序已于 2026-09-19 替换为本工程编译的 v1.1.0.0

### 变更

- **配置存储**：注册表 → `settings.ini`（注册表只读一次用于迁移）
- 结果计数 `已抽取:N个` 现在无论是否勾选「不允许重复」都会累加（原来不勾选时恒为 0）
- 摇号特效的滚动数字改用"抽号前剩下的池子"来显示，不会闪出本轮已经抽过的号码
- 号码分隔符额外支持**全角空格**

### 修复

- **区间过大导致界面卡死**：原来算了 `100000` 的上限却没真正用上（死代码），现在超过 10 万个号码会直接提示而不是硬抽
- 原 `WeightedPick` 里"总权重为 0"分支会连续取两次随机数（选中一个、删掉另一个）的逻辑错误，
  改写为选中即删同一个下标（该分支原本不可达，属隐患）
- 隐藏入口码 `1016` 不会再被当成"开始号码"写进配置文件

### 安全说明

- 程序不联网、不请求管理员权限，只读写自己的 `settings.ini`（及首次运行时的注册表读取）

---

## [0.1.0] - 2026-09-19

### 新增

- 从原 `幸运之子——摇号机.exe` 反汇编（dnfile + dncil 读 IL）**逐条还原**出的可编译 C# 源码工程：
  - `src/Program.cs`、`src/Config.cs`、`src/MainForm.cs`、`src/AdminPanel.cs`、`src/AssemblyInfo.cs`
  - 界面与行为与原程序一致，作为后续改造的基线
- `build.cmd` / `build.ps1`：用 Windows 自带的 `csc.exe` 编译（无需 .NET SDK / Visual Studio），脚本位置无关
- `assets/app.ico`：从原 exe 提取的程序图标（16×16 + 32×32）
- `README.md`：还原过程、原程序行为、构建与使用说明

### 验证

- 控件树（类名 / 文本 / 坐标 / 尺寸）与原程序 **15 项全部一致**
- 界面截图与原程序**像素级完全一致**（ImageMagick `AE=0`、`RMSE=0`）
- 装配名 `幸运之子——摇号机`、CLR v4.0.30319、32 位 PE、图标与版本资源均一致
